<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">

    <changeSet id="0010-1" author="cognito-integration">
        <addColumn tableName="users">
            <column name="cognito_sub" type="varchar(36)"/>
            <column name="first_name" type="varchar(100)"/>
            <column name="last_name" type="varchar(100)"/>
            <column name="email" type="varchar(320)"/>
        </addColumn>
        <addUniqueConstraint
            tableName="users"
            columnNames="cognito_sub"
            constraintName="uq_users_cognito_sub"/>
        <addUniqueConstraint
            tableName="users"
            columnNames="email"
            constraintName="uq_users_email"/>
    </changeSet>

    <!-- Backfill antes de remover as colunas antigas.
         username não é email real nos seeds, então adiciona domínio placeholder.
         cognito_sub fica NULL nos usuários existentes — PostgreSQL permite múltiplos NULLs em UNIQUE. -->
    <changeSet id="0010-2" author="cognito-integration">
        <sql>
            UPDATE users
            SET
                email      = username || '@wsssguardo.local',
                first_name = split_part(full_name, ' ', 1),
                last_name  = CASE
                                 WHEN full_name LIKE '% %'
                                 THEN substring(full_name FROM position(' ' IN full_name) + 1)
                                 ELSE NULL
                             END
            WHERE email IS NULL;
        </sql>
    </changeSet>

    <changeSet id="0010-3" author="cognito-integration">
        <dropColumn tableName="users" columnName="full_name"/>
        <dropColumn tableName="users" columnName="username"/>
    </changeSet>

</databaseChangeLog>
